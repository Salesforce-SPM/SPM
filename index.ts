// import Commands = require('./src/commands');
import Controllers = require('./src/controller');
import Interfaces = require('./src/interface');
import Utils = require('./src/utils');

const exp = {
    // command: Commands,
    controller: Controllers,
    interface: Interfaces,
    util: Utils,
}